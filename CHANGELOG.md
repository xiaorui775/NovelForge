# 更新日志

## 2026-05-24

**新功能**

- **结构化章节卡片** — 聊天 full 模式用 ChapterSummary 结构化字段（角色状态、悬念、时间线等）替代自由文本摘要，同样信息量 token 消耗降低约 60%
- **temperature/top_p 全链路传递** — 续写、多轮生成、头脑风暴、精修、改写选段均支持从前端滑块到 LLM API 的温度参数传递，之前仅单轮生成支持
- **pg_trgm 全文检索** — Chapter.content 建立 GIN trigram 索引，模糊搜索性能大幅提升，结果按相似度排序
- **拆分章节弹窗改进** — 拆分模态框改为固定居中覆盖层，解决弹窗不可见的问题

**工程优化**

- 新增 `format_chapter_card` 共享 helper，统一聊天、一致性检查、伏笔扫描三处的章节摘要格式化逻辑
- `openai_adapter.generate()` 补齐 temperature/top_p 支持，与 `generate_stream()` 对齐

---

## 2025-05-23

**新功能**

- **系列管理** — 创建系列、关联项目并排序，系列续作自动注入前作上下文，保持故事连贯
- **写作助手增强** — 支持引用章节全文或选段作为上下文对话；AI 改写建议支持多条 ACTION 一键应用，自定义确认弹窗替代浏览器 alert
- **章节大纲拆分/合并** — 在指定段落后一分为二，或合并相邻章节，章节号自动重排
- **反向大纲** — AI 对比计划大纲与实际内容，发现偏差
- **后写综合分析** — 合并质量评分、一致性检查、节奏分析、伏笔扫描、内容摘要、故事圣经沉淀为一次 AI 调用，节省 token
- **编辑器增强** — temperature / topP 实时调节；选段工具栏支持对选中文字快速操作；预览对比面板重新设计
- **分析面板改进** — 日/周词速统计（按章节去重）、完成度预测、角色关系图、伏笔时间线
- **角色弧线追踪** — 角色在各章节中的状态变化追踪
- **一致性检查增强** — 跨章节一致性扫描
- **成本估算** — 生成前预估 token 和费用

**Bug 修复**

- AI 改写 ACTION 标记含嵌套花括号时解析失败
- 统计面板日/周词数因版本重复计入偏高
- 合并章节时 ORM 级联删除未触发，遗留孤立数据
- 采纳预览版本重复创建版本记录
- 编辑器 temperature / topP 滑块变更后生成未生效
- 丢弃预览仅清前端状态，刷新后重新出现
- 写作助手应用 ACTION 失败后客户端索引与服务端不同步
- expand-detail / generate-outline 等 AI 接口前端 30s 超时不够，调整为 120-180s
- expand-detail 大纲摘要过长导致提示词溢出

**工程优化**

- 合并 alembic 迁移文件 002 + 003 为单个增量迁移
- AI 调用内部复用 adapter 连接，减少 httpx 开销
- 生成服务异常日志从静默吞掉改为 warning 级别记录
